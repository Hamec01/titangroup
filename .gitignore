.next
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
.vercel
.env.local

*.tsbuildinfo

.env
.env.*
!.env.example
!.env.titanor-time.example

# Database dumps (real data, never committed)
/backups

# Local runtime upload dir (lib/employee-files.ts writes here; prod uses a bind mount).
# Never committed — contains employee photos / documents.
/titanor-time-app/uploads/
/uploads/

# R06-B Docker build outputs — regenerated in the builder stage on every image build
# (scripts/build-runtime-scripts.mjs, scripts/assemble-prisma-tools.mjs).
/titanor-time-app/.runtime/
/titanor-time-app/.prisma-tools/

# Next.js auto-generates this on every build / version bump (see its own header).
/titanor-time-app/next-env.d.ts
/next-env.d.ts
